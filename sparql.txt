https://github.com/A-I-18/PokeOntology/blob/master/SPARQL_queries.rq
https://github.com/A-I-18/PokeOntology/blob/master/query.py

# Retorna as triples
SELECT ?s ?p ?o  WHERE {?s ?p ?o .}

# Conta as classes da Ontologia
SELECT (COUNT(?x) AS ?count)
WHERE {
    ?x a owl:Class . 
}

# Retorna danos morais
SELECT DISTINCT ?acao 
WHERE {
    ?acao rdf:type ?type .
    ?type rdfs:subClassOf* OntologiaLegal:Ação .
    ?acao OntologiaLegal:temMotivo ?m .
    FILTER(?m = "Danos Morais") .
}